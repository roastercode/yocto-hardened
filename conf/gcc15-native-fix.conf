# Fix GCC 15 C23 compatibility for native packages
# 'int' is now reserved in C23, use -std=gnu17 for legacy C code
CFLAGS:append:class-native = " -std=gnu17"
CXXFLAGS:append:class-native = " -std=gnu++17"
